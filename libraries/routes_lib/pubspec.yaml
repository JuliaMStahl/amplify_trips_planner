name: routes_lib
description: A new Flutter module project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  home:
    path: ../../features/home
  trip:
    path: ../../features/trip
  edit_trip:
    path: ../../features/edit_trip

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.routes_lib
    iosBundleIdentifier: com.example.routesLib
